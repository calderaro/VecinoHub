import { test } from "../../playwright/fixtures/auth";

test.describe("resident members", () => {
  test.fixme("group member sees roster only", async () => {});
  test.fixme("group admin sees management controls", async () => {});
  test.fixme("group admin adds member by email", async () => {});
  test.fixme("group admin promotes member to group_admin", async () => {});
  test.fixme("group admin demotes member to group_member", async () => {});
  test.fixme("group admin removes member", async () => {});
  test.fixme("neighborhood admin can manage members of scoped group", async () => {});
  test.fixme("resident cannot manage foreign group", async () => {});
});
