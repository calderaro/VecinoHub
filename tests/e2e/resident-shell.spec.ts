import { test } from "../../playwright/fixtures/auth";

test.describe("resident shell", () => {
  test.fixme("dashboard shows group selector for user with groups", async () => {});
  test.fixme("dashboard no-group state renders for user without groups", async () => {});
  test.fixme("no-group state allows signout", async () => {});
  test.fixme("group dashboard overview renders", async () => {});
  test.fixme("group dashboard nav opens each section", async () => {});
  test.fixme("user menu opens and closes with esc", async () => {});
  test.fixme("user menu shows dashboard and profile entries", async () => {});
  test.fixme("group switcher changes route", async () => {});
});
