import { test } from "../../playwright/fixtures/auth";

test.describe("platform neighborhoods", () => {
  test.fixme("platform dashboard renders list and create form", async () => {});
  test.fixme("platform admin creates neighborhood", async () => {});
  test.fixme("new neighborhood appears in platform list", async () => {});
  test.fixme("platform admin opens neighborhood detail", async () => {});
  test.fixme("platform admin edits neighborhood", async () => {});
  test.fixme("platform admin adds member by existing email", async () => {});
  test.fixme("platform admin updates member role", async () => {});
  test.fixme("platform admin updates member status", async () => {});
  test.fixme("platform admin removes neighborhood member", async () => {});
  test.fixme("platform admin deletes disposable neighborhood", async () => {});
  test.fixme("neighborhood admin cannot access platform dashboard", async () => {});
});
