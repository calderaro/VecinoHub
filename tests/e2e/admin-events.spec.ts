import { test } from "../../playwright/fixtures/auth";

test.describe("admin events", () => {
  test.fixme("events list renders", async () => {});
  test.fixme("admin creates event", async () => {});
  test.fixme("admin edits event", async () => {});
  test.fixme("admin clears optional end date", async () => {});
  test.fixme("admin deletes event", async () => {});
  test.fixme("resident can view created event in dashboard", async () => {});
  test.fixme("non-admin cannot access event admin routes", async () => {});
});
