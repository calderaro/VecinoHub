import { test } from "../../playwright/fixtures/auth";

test.describe("profile and localization", () => {
  test.fixme("profile page renders expected controls", async () => {});
  test.fixme("profile update persists username", async () => {});
  test.fixme("profile update persists language", async () => {});
  test.fixme("changing language to english updates dashboard shell", async () => {});
  test.fixme("changing language to spanish updates dashboard shell", async () => {});
  test.fixme("profile values persist after refresh", async () => {});
});
