import { test } from "../../playwright/fixtures/auth";

test.describe("resident fundraising", () => {
  test.fixme("fundraising list renders search and table", async () => {});
  test.fixme("campaign detail renders resident-scoped contributions", async () => {});
  test.fixme("resident submits cash contribution", async () => {});
  test.fixme("resident submits wire transfer contribution", async () => {});
  test.fixme("wire transfer validation requires reference and date", async () => {});
  test.fixme("resident deletes own contribution while campaign open", async () => {});
  test.fixme("resident cannot submit to closed campaign", async () => {});
  test.fixme("resident cannot access campaign from foreign group", async () => {});
  test.fixme("resident sees admin-reviewed status after refresh", async () => {});
});
