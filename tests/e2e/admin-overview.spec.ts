import { test } from "../../playwright/fixtures/auth";

test.describe("admin overview", () => {
  test.fixme("neighborhood admin can access admin landing", async () => {});
  test.fixme("admin landing lists authorized neighborhoods", async () => {});
  test.fixme("admin overview renders stats cards", async () => {});
  test.fixme("admin overview renders recent sections", async () => {});
  test.fixme("neighbor cannot access admin", async () => {});
  test.fixme("admin cannot access foreign neighborhood overview", async () => {});
});
