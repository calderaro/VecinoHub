import { test } from "../../playwright/fixtures/auth";

test.describe("admin polls", () => {
  test.fixme("polls list renders", async () => {});
  test.fixme("admin creates draft poll with initial options", async () => {});
  test.fixme("poll detail shows options list", async () => {});
  test.fixme("admin adds option from detail page", async () => {});
  test.fixme("admin edits option", async () => {});
  test.fixme("admin removes option", async () => {});
  test.fixme("admin launches poll", async () => {});
  test.fixme("admin closes poll", async () => {});
  test.fixme("admin reopens poll", async () => {});
  test.fixme("admin resets poll to draft and clears votes", async () => {});
  test.fixme("poll results render for voted poll", async () => {});
  test.fixme("non-admin cannot access poll create/edit", async () => {});
});
