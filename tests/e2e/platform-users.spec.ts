import { test } from "../../playwright/fixtures/auth";

test.describe("platform users", () => {
  test.fixme("platform user directory renders", async () => {});
  test.fixme("platform user search works", async () => {});
  test.fixme("platform user role filter works", async () => {});
  test.fixme("platform user status filter works", async () => {});
  test.fixme("platform admin opens user detail", async () => {});
  test.fixme("platform admin updates system role", async () => {});
  test.fixme("platform admin updates system status", async () => {});
  test.fixme("deactivating user kills active session", async () => {});
  test.fixme("inactive user cannot sign in again", async () => {});
  test.fixme("platform admin edits user profile", async () => {});
  test.fixme("edited profile persists on detail page", async () => {});
});
