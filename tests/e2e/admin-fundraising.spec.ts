import { test } from "../../playwright/fixtures/auth";

test.describe("admin fundraising", () => {
  test.fixme("fundraising list renders", async () => {});
  test.fixme("admin creates campaign", async () => {});
  test.fixme("campaign detail renders progress and contributions", async () => {});
  test.fixme("per-group amount computed after create", async () => {});
  test.fixme("admin edits campaign goal and amount recalculates", async () => {});
  test.fixme("admin closes campaign", async () => {});
  test.fixme("admin submits contribution from admin contribute page", async () => {});
  test.fixme("admin confirms submitted contribution", async () => {});
  test.fixme("admin rejects submitted contribution", async () => {});
  test.fixme("resident sees reviewed contribution status", async () => {});
  test.fixme("non-admin cannot access admin campaign routes", async () => {});
});
