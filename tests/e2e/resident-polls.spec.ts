import { test } from "../../playwright/fixtures/auth";

test.describe("resident polls", () => {
  test.fixme("polls list renders with search", async () => {});
  test.fixme("poll detail renders", async () => {});
  test.fixme("resident can vote in active poll", async () => {});
  test.fixme("resident vote persists after refresh", async () => {});
  test.fixme("resident can change vote and still has one stored vote outcome", async () => {});
  test.fixme("closed poll blocks voting ui", async () => {});
  test.fixme("resident cannot access foreign group poll route", async () => {});
  test.fixme("resident list excludes non-active polls", async () => {});
});
