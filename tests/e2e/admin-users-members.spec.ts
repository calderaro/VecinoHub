import { test } from "../../playwright/fixtures/auth";

test.describe("admin users and members", () => {
  test.fixme("admin users list renders scoped grouped users only", async () => {});
  test.fixme("admin users search works", async () => {});
  test.fixme("admin users status filter works", async () => {});
  test.fixme("users without active group membership in neighborhood are absent", async () => {});
  test.fixme("neighborhood admin can open scoped user detail", async () => {});
  test.fixme("neighborhood admin detail is read-only", async () => {});
  test.fixme("admin members page renders only neighborhood admins", async () => {});
  test.fixme("admin adds existing user as neighborhood admin", async () => {});
  test.fixme("admin edits neighborhood admin role/status", async () => {});
  test.fixme("admin removes neighborhood admin membership", async () => {});
  test.fixme("membership inactivation revokes stale group-admin ability", async () => {});
});
