import { test } from "../../playwright/fixtures/auth";

test.describe("admin posts", () => {
  test.fixme("posts list renders", async () => {});
  test.fixme("admin creates draft post", async () => {});
  test.fixme("admin edits draft post", async () => {});
  test.fixme("admin publishes post", async () => {});
  test.fixme("published post becomes visible to resident", async () => {});
  test.fixme("admin unpublishes post", async () => {});
  test.fixme("admin deletes post", async () => {});
  test.fixme("schedule later exposes current unsupported behavior", async () => {});
});
