import { test } from "../../playwright/fixtures/auth";

test.describe("admin groups", () => {
  test.fixme("groups list renders", async () => {});
  test.fixme("admin creates empty group", async () => {});
  test.fixme("admin creates group with assigned admin", async () => {});
  test.fixme("admin edits group", async () => {});
  test.fixme("admin deletes group", async () => {});
  test.fixme("group detail member management works from admin", async () => {});
  test.fixme("group search filters list", async () => {});
});
