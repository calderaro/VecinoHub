import { test } from "../../playwright/fixtures/auth";

test.describe("public", () => {
  test.fixme("landing renders for signed-out user", async () => {});
  test.fixme("landing login cta opens login page", async () => {});
  test.fixme("landing legal links resolve", async () => {});
  test.fixme("legal hub navigation works", async () => {});
  test.fixme("authenticated root redirects to dashboard", async () => {});
});
