import { test } from "../../playwright/fixtures/auth";

test.describe("resident events and posts", () => {
  test.fixme("events list renders", async () => {});
  test.fixme("event detail renders title date location description", async () => {});
  test.fixme("posts list renders", async () => {});
  test.fixme("post detail renders published content", async () => {});
  test.fixme("resident cannot open draft post detail", async () => {});
  test.fixme("resident cannot access foreign group event route", async () => {});
  test.fixme("resident cannot access foreign group post route", async () => {});
});
